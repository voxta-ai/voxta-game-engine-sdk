# Continuation Prompt: Write the Unity quickstart

Continue in `G:\voxta-projects\GitHub\voxta-game-engine-sdk\unity`. Preserve all
existing uncommitted work.

M1-M4 and the first two M5 units are complete. BasicIntegration uses device-flow
authorization, persisted tokens, authenticated reply-audio downloads, scrollable
diagnostics, microphone voice input, spatial reply playback, and a visible `wave`
action callback. Its imported copy in `G:\Unity\voxtaSDK-sandbox\Assets\Samples\...`
is separate from `Samples~`.

Implement only the next M5 unit: write quickstart documentation that gets a new
Unity user to a talking companion in under 15 minutes. Inspect the package README,
BasicIntegration README and scene, device authorization flow, and current local-server
guidance first. Cover installation, sample import, companion configuration, device
approval, text and voice interaction, Unity spatial playback, the `wave` action, and
the practical local-server audio/STT settings required for those paths.

Do not change runtime code, protocol or auth contracts, the prefab, sample scene,
CI, build-target work, package-install validation, or later M5 items. Update only
directly affected documentation, validate the instructions against the existing
sandbox where possible, and mark only the quickstart M5 plan item after its stated
verification passes.
